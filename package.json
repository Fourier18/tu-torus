{
  "name": "coding-tutor",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "piston": "docker compose -f piston/docker-compose.yaml up -d",
    "dev": "concurrently -n server,frontend -c blue,green \"npm --prefix server start\" \"npm --prefix frontend run dev\"",
    "setup": "npm --prefix server install && npm --prefix frontend install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "concurrently": "^10.0.5",
    "electron": "^44.4.5",
    "electron-builder": "^26.15.3"
  }
}
